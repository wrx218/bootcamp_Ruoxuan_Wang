# Homework 02: Tooling Setup

This directory contains a reproducible Python project scaffold created to practise environment setup, dependency management, secret handling with `python-dotenv`, Jupyter notebook execution, and Git/GitHub workflow. Empty project directories are preserved with `.gitkeep` files, configuration values are loaded through `src/config.py`, and the setup notebook verifies both the dummy API key and a small NumPy operation.
